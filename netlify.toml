[build]
  publish = "public"
  functions = "netlify/functions"
  command = "npm install"

[functions]
  node_bundler = "esbuild"

# Supaya frontend bisa panggil /api/xxx alih-alih /.netlify/functions/xxx
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
